version: 2
syncs:
  backend-sync:
    src: './backend'
    sync_strategy: 'native_osx'
    sync_excludes: ['tmp','.git', '*.log']
  frontend-sync:
    src: './frontend'
    sync_strategy: 'native_osx'
    sync_excludes: ['.git', '*.log']
